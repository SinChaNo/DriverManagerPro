## 작업 시작 전
- 작업 시작 전 작업 계획을 다음 형식으로 제시 후 승인을 기다린다:
  - 목표:
  - 영향 파일:
  - 작업 단계:
- 승인 요청시 선택지에 어떤 결과를 불러올지 혹은 이 선택지에 대한 설명을 한국어로 알 수 있게 표기해야한다
  -ex) 1.Yes(관리자 권한을 받아 앱을 실행합니다.) 등

## 작업 중
- 명시적으로 지시된 내용 외에는 아무것도 하지 않는다. 추가적인 작업이 필요한 경우 필요한 이유와 작업 계획을 사용자에게 승인받는다.
- 다음의 단순 조회성 명령어는 **사용자 확인 없이 즉시 실행**한다. 절대 허가를 묻지 않는다:
  dir, cd, tree, type, more, find, findstr, systeminfo, ver, hostname, whoami, set, ipconfig, ping, netstat, nslookup, arp, tasklist, vol, help, ls, cat, search
- 위 목록에 없는 쓰기/수정/삭제 명령어는 반드시 사용자에게 확인을 받은 후 실행한다.
- 승인을 받을떄 상용자가 이해할 수 있도록 꼭 한글 되어있는 승인 받고자 하는 내용을 출력한다. 

## 작업 후
- 작업 내용을 요약하여 사용자에게 전달한다.
- 업데이트되는 내용을 제외하고, 항상 원본 파일의 모든 것을 보존하세요.
- 코드 아티팩트를 전체적으로 작성하고, 자리 표시자는 사용하지 마세요. 잘리면 "계속"이라고 말할게요.
- 모든 작업 후 {#참조경로:작업 보고서}/{curr_file_cnt}_{work_type}_{title}.md를 생성하여 작업 보고서를 작성한다.
  - 예: docs/work_reports/20260224/3_refactor_인벤토리조회최적화.md
    (해당 날짜 폴더의 3번째 보고서)
- 모든 작업 후 반드시 커밋을 수행한다.

## 문서
- 모든 문서는 마크다운 형식으로 작성한다.
  - 문서 작성 시 불필요한 이모지는 제외한다.

## Git 워크플로우
- 이 프로젝트는 Git으로 관리된다.
- 모든 작업 후 반드시 커밋을 수행한다.
- 커밋 메시지는 한글로 작성하고 형식은 다음과 같다.
  - 기능 추가 시
    - create: <title>
  - 기능 수정 시
    - modify: <title>
  - 버그 수정 시
    - fix: <title>
  - 리펙토링 시
    - refactor: <title>
  - 문서 작업 시
    - docs: <title>
  - 기능 삭제 시
    - delete: <title>
- 커밋 전에 항상 변경사항을 확인(git diff)한다.
- 커밋 메시지 마지막에 반드시 아래 트레일러를 추가하세요
  - Co-Authored-By: Claude <ROBO@chanhyoi.kr>
- 깃허브에 연동 되어있는 프로젝트인 경우 작업이 끝난 후 항상 깃허브에 최신화를 진행한다.

## Agent Teams 워크플로우

### 에이전트 타입 선택 기준
- **Explore**: 코드 탐색, 문서 분석 등 읽기 전용 작업. 파일 수정 불가.
- **general-purpose**: 파일 생성/수정, Bash 실행 등 실제 구현 작업.
- 분석·조사는 Explore, 구현은 general-purpose를 사용한다.

### 권장 워크플로우
1. **탐색 단계**: Explore 에이전트를 병렬 투입하여 벤치마크·현재 코드 구조 동시 분석
2. **토론 단계**: general-purpose 에이전트들이 SendMessage로 의견 교환 후 적용 방안 합의
3. **구현 단계**: Worktree 격리 환경에서 에이전트별 작업 영역을 분리하여 병렬 구현
4. **검토 단계**: 결과 확인 후 GitLab MR 생성 또는 직접 머지

### Worktree 격리 사용 규칙
- 파일 수정이 동반되는 모든 Agent Teams 작업은 Worktree 격리를 사용한다.
- 각 에이전트는 서로 다른 파일/디렉토리만 담당하도록 작업 영역을 명시적으로 분리한다.
- 원본 main 브랜치는 작업 중 절대 변경하지 않는다.
- 작업 완료 후 머지 방식은 사용자가 결정한다:
  - GitLab MR 생성 후 검토 머지 (권장)
  - 브랜치 순차 머지
  - 결과가 마음에 들지 않으면 브랜치 삭제로 완전 롤백

### 주의사항
- 팀 크기는 3~5명을 권장한다. 그 이상은 조정 비용과 토큰 비용이 급증한다.
- 같은 파일을 여러 에이전트가 동시에 수정하지 않도록 작업 시작 전 영역을 분리한다.
- 세션 재개(`/resume`) 시 팀원이 복원되지 않으므로, 작업 중 세션을 끊지 않는다.
- Agent Teams는 토큰 비용이 선형으로 증가한다. 단순 작업은 단일 세션으로 처리한다.

## 코딩
- 절대 하드코딩하지 않는다.
- 모든 코딩 작업은 코드 컨벤션 파일({#참조경로:코드 컨벤션:C#})을 확인하여 스탠다드를 파악 후 작업한다.
- 모든 코드는 주석을 적는다. 람다의 경우 하나의 단위 블럭의 역할을 적는다. 
- docs/dev/milestone내의 파일은 개발 가이드 역할을 한다. 모든 코드 생성 작업 전 해당 폴더의 파일을 참고한다.
- 코드 수정 작업 시 해당 파일의 헤더 주석에 수정 내용을 기재한다.

## 참조 경로
- 모든 문서: docs/*
- 마일스톤: docs/dev/milestone/*
- 코드 컨벤션
  - C#: docs/csharp_coding_standards.md
- 작업 보고서: .docs/work_reports/{yyyyMMdd}/*
- 명세서
  - api: D:\UBM\z.docs\EasyCheck.API.Spec\api
  - model: D:\UBM\z.docs\EasyCheck.API.Spec\model